name = "train-times"
main = "src/index.js"
compatibility_date = "2024-12-16"
workers_dev = true

# Secret required (set via `wrangler secret put RTT_PORTAL_TOKEN --name train-times`):
#   RTT_PORTAL_TOKEN : long-lived portal JWT from api-portal.rtt.io
#                      Used server-side to exchange for a short-lived access token.
#                      Find it in the "API Authorisation" section of api-portal.rtt.io.
